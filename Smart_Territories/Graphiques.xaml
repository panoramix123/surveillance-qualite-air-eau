<Page x:Class="Smart_Territories.Graphiques"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Graphiques">

    <Grid Background="Gray">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="20">

                <TextBlock Text="Évolution des polluants (7 derniers jours)" 
                           FontFamily="Fonts/#Dosis" FontSize="28" FontWeight="Bold" Foreground="Black" Margin="10,0,0,15"/>

                <ItemsControl x:Name="ChartsContainer">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="350" Height="220" Background="DarkGray" CornerRadius="8" Padding="15" Margin="10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="{Binding Title}" FontFamily="Fonts/#Dosis" FontSize="16" FontWeight="Bold" Foreground="Black"/>

                                    <ItemsControl Grid.Row="1" ItemsSource="{Binding Points}" VerticalAlignment="Bottom" Height="150">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <UniformGrid Rows="1"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel VerticalAlignment="Bottom" Margin="2,0">
                                                    <TextBlock Text="{Binding Value}" FontFamily="Fonts/#Dosis" FontSize="11" Foreground="Black" HorizontalAlignment="Center"/>

                                                    <Rectangle Height="{Binding DisplayHeight}" Fill="#333333" Width="30" RadiusX="2" RadiusY="2" Margin="0,2,0,2"/>

                                                    <TextBlock Text="{Binding Label}" FontFamily="Fonts/#Dosis" FontSize="11" Foreground="#222222" HorizontalAlignment="Center"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>